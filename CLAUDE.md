# このプロジェクトで作業するときの入口

**最初に `引き継ぎ書.md` を全文読むこと。** 作業を始める前に。

そこに、何を作っているか・公開先・ビルド手順・**絶対に守る前提**・未着手事項がまとまっている。
詳細の正本は `README.md`（約72KB）だが、まず引き継ぎ書で全体像を掴んでから降りること。

## 特に踏みやすい地雷（詳細は引き継ぎ書の4節）

- **50音歴の各音に意味・属性・対応表を付けない。** 音と日付だけ。ここはユーザーの領域
- **太陽＝火／月＝水 のような火水の割り当てをしない。** 火水の割り当てはユーザーの領域
- **五行と火水二元を対応づけない。** 別体系として切り離す
- `~/MUNATO/MUNATO_OS/` は読むのは可。**明示指示なしに変更しない**
- 確信をもって固定できない表は、推測で埋めず `[不能]` に置く
- 人物理解が指示書 `birth-cycle-reflection` と食い違っているのは**意図的**。
  バグとして直さないこと（README「指示書からの意図的な逸脱」参照）

## 触る場所

`src/` を直して、`node build.js` → `node make-pwa.js` → `node make-artifact.js`。
`solar-system.html` / `dist/` / `docs/` は**生成物なので直接編集しない**。
文面を足すときは `node test/run-all.js` の禁止事項検査も一緒に更新する。
