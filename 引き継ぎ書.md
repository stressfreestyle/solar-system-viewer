# 引き継ぎ書 — 太陽系リアルタイムビューア

最終更新: 2026-08-14

**新しいセッションはこのファイルから読むこと。** 詳細は `README.md`（約72KB）にあるが、
まずここで全体像と「触ってはいけない前提」を掴んでから README に降りるのが速い。

---

## 1. これは何か

太陽を中心とした8惑星の位置をリアルタイムで見るスマホ向けアプリ。
段階的に育って、いまは次の3つを持つ。

1. **天体** — 8惑星＋月の位置。中心天体を切り替えると見かけの運動（逆行のループ）が見える
2. **暦** — 50音歴・二十四節気・七十二候・旧暦・二十七宿・月相と、それらを構造として読む節
3. **人物理解** — 生年月日から四柱推命・算命学・宿曜で読み解く

**外部通信ゼロ・ライブラリ不使用。** 単一コードベースから3つの配布形態を生成する。
ビルドが外部参照・通信API・絶対パスを機械検査して、混ざっていたら落ちる。

## 2. 公開先

| 形態 | 場所 |
|---|---|
| **PWA版（主）** | https://stressfreestyle.github.io/solar-system-viewer/ |
| リポジトリ | https://github.com/stressfreestyle/solar-system-viewer （**公開**） |
| 単一HTML版 | `solar-system.html`（AirDrop用。`file://` で全機能が動く） |
| Artifact版 | URLは `LOCAL_NOTES.md`（gitignore済み。非公開リンクなので公開リポジトリに載せない） |

## 3. 作業のしかた

```bash
cd "projects/開発/20260812_太陽系リアルタイムビューア"
node build.js          # → solar-system.html
node make-pwa.js       # → dist/ と docs/（docs/ は Pages の公開先。自動同期）
node make-artifact.js  # → solar-system.artifact.html
node test/run-all.js   # 全テスト（7ファイル）
```

**`src/` を直す。`solar-system.html` / `dist/` / `docs/` は生成物なので直接編集しない。**

公開は `git add -A && git commit && git push` → 1〜2分で反映。
`gh api repos/stressfreestyle/solar-system-viewer/pages --jq .status` が `built` になれば完了。

### テストが品質の担保になっている

このプロジェクトは「機械検査で縛る」方針で育ってきた。文面を足すときも、
**禁止事項の検査を一緒に更新する**こと。現在の7ファイルが見ているもの:

- 惑星位置の精度、ケプラー方程式の残差
- 暦の検算（朔・旧暦・節気・七十二候・50音歴・閏月）
- 四柱推命の干支と境界（立春・節入りの前後1分）
- 文面の禁止事項（後述）
- 描画の構造（断定と根拠の対応、1タップ到達、用語説明の網羅）

---

## 4. 絶対に守る前提 — ここを破ると作り直しになる

### 4-1. ユーザーの領域に踏み込まない

| 対象 | 禁止事項 |
|---|---|
| **50音歴の各音** | 意味・属性・対応表を**一切付けない**。音と日付・区間だけ出す |
| **火水の割り当て** | 太陽＝火／月＝水 のような割り当てを**しない**。出せるのは「どちらが補正を要するか」という計算上の非対称だけ |
| **五行と火水二元** | **対応づけない**。五行は四柱推命の内側の記号。同じ字を使う別体系として切り離す |
| **`~/MUNATO/MUNATO_OS/`** | 読むのは可。**明示指示なしに変更しない**（ユーザー自身が定めた規則） |

これらは繰り返し確認された方針。**推測で補完しないことがこのプロジェクトの最重要の価値。**

### 4-2. 確信をもって固定できないものは `[不能]` に置く

推測で埋めると、誤った構造を事実として出すことになる。現在 `[不能]` は9件
（時柱・真太陽時・人体星図の配置・大運の順逆・身強身弱・格局用神喜忌・宿ごとの人物像・
魂の目的・性別からの算出）。理由つきで画面に出している。

### 4-3. 文面の禁止事項（機械検査あり）

- 死・病気・事故・犯罪・妊娠出産・離婚・財産などの高リスク事項を予測しない
- 医療・法律・採用・信用・結婚などの判断へ誘導しない
- 的中率・確率・スコア・パーセンテージを出さない
- 吉凶・開運・「今日は〜に良い日」を書かない
- 職業名を直接列挙しない（「場・役割の条件」として書く）
- **本人を否定する表現を書かない**（気づいていない／自覚がない／本当は〜のはずだ／勘違い）
- 誰にでも当てはまる抽象文（バーナム文）を書かない

### 4-4. 人物理解の構造（第5フェーズで確定）

```
主   = 法則（真理構造）
従1  = 生年月日・暦記号・星の配置
従2  = 生活・履歴・環境・選択・行動
```

法則を従1に照らして従2を読み解く。食い違ったときは**法則側を優先**する
（本人が、反証不能になるという指摘を受けたうえで明示的に選択した設計）。
ただし「本人が間違っている／気づいていない」とは書かず、
「展開が別の面に出ているか、まだその面が見えていない」として扱う。

**この設計は指示書 `~/.claude/skills/birth-cycle-reflection/` の中心規定と意図的に食い違っている。**
README の「指示書からの意図的な逸脱」に経緯と維持項目を記録済み。
**バグとして「直さない」こと。**

### 4-5. 表と裏（第9フェーズ）

**表** = 一般向けの読み解きの散文・命式表・`[不能]`・状態ボタン
**裏** = 法則（アルゴリズム）・根拠・但し書き。「法則」チップのポップアップへ

断定ブロックは**根拠なしでは作れない構造**。`rd()` が根拠を裏へ登録してチップを付ける。
節を足す手順は `src/reading-view.js` の冒頭コメントにある。

---

## 5. プロジェクトの外にある関連物

このフォルダの外にも、この作業で作った／参照するものがある。

| 場所 | 内容 |
|---|---|
| `~/MUNATO/CLAUDE.md` | 「MUNATO OS 真理構造指示」章を追記済み（判断順序・禁止事項・出力方針） |
| `~/MUNATO/.claude/skills/munato-reading/SKILL.md` | 読み解き手順のスキル。**新規作成**。参照資料19件の実在パス入り |
| `~/.claude/skills/birth-cycle-reflection/` | 人物理解の元になった指示書。**書き換えていない** |
| `~/MUNATO/MUNATO_OS/00_基準/真理構造宣言.txt` | 「真理構造を正中に置け」。読むだけ |
| `~/MUNATO/MUNATO_OS/00_基準/ANCHOR_宿曜道法則_読解基準.txt` | **宿だけでの人格固定断定を禁じている**。宿曜を触るなら必読 |
| `宿曜経_典拠調査.md`（このフォルダ） | 大正蔵 T1299 を実際に取得して全巻通読した調査記録。**宿曜実装の根拠はこれ** |

`~/MUNATO/` は毎日23時に自動で git バックアップ（プライベートリポジトリへ push）。

---

## 6. 未着手・未決事項

### 6-1. 宿曜の4項目（典拠調査済み・実装待ち）

`宿曜経_典拠調査.md` で原典を確認済み。**調査前の想定は覆った** —
三九の秘法も六害宿も宿ごとの人物像も、すべて原典（T1299）に実在する。

実装できる: 二十七宿ごとの短い人物像（各40〜60字）／性類7分類／三九の秘法
（命業胎＋栄衰安危成壊友親、27位置の対応、相性判定、双方向で見る作法）／
六害宿（命事意聚同克、1・4・10・13・16・20）／宿ごとの日の宜・不宜

**実装するなら必須の注記**（詳細は調査記録の「画面に明記すべき文言」）:
- 大正蔵本（大陸本系統）に拠ること。和本には異同がある
- 原典は「法合」＝傾向の表現で、断定形ではない
- 六害宿は「七曜がその宿を犯したとき」のみ発動する条件付きの凶
- 本命宿の算出が原典（望基準）と日本の暦注（朔日基準）で異なる。現在の実装は朔日基準

**実装してはいけない**: 現代書の詳細な人物像（原典の記述量を桁で超える）／
相性の数値化／関係名の情緒的な意味づけ／凌犯期間の具体的日付／昭和の復興者の系譜

### 6-2. 状態ボタンが記録止まりになっている

「まだ見ていない／この面に出ている／別の面に出ている／判断がつかない」は、
**選んでも読み解きの文章は変わらず、集計も履歴もない**。保存はオプトイン時のみ。
ユーザーに3案を提示して未回答:
(a) 日付つきの履歴にする (b)「別の面に出ている」を集めた一覧を作る
(c) 具体的な出来事を書き込む欄を足す — あるいは (d) ボタン自体を外す

### 6-3. 出生地・性別が読みに影響しない

入力欄はあるが計算に使われていない。出生地は `[不能]` の文面が変わるだけ、
性別は呼称の表示のみ。実装候補:
- **真太陽時の補正**（経度＋均時差で最大45分。刻の境目なら時柱が1つ変わる）
- **国外の出生地**（現在JST固定。国外生まれは合わない）← 影響が大きい
- **大運の順逆**（旧来の男女二分法前提。明示した別枠でのみ）

### 6-4. その他

- **二十七宿の天文方式**（比較資料側）の起点とアヤナムシャは、ユーザー未確認のまま。
  `NAKSHATRA_CONFIG` 1か所で切替可能
- 指示書 `birth-cycle-reflection` は二層のまま。アプリは三層。揃えるかは未決
- 惑星ラベル（キャンバス描画）は文字倍率の対象外。拡大要望があれば別途

---

## 7. 運用上の注意（実際に踏んだもの）

- **セッション上限に複数回当たった。** サブエージェントが作業途中で落ちる。
  対策として「小さく作って常にビルドが通る状態を保ち、区切りごとに README の
  経緯欄へ `[済]` を残す」ようにした。これで再開時に進捗が失われない
- サブエージェントの文脈は失われることがある。**README を起点に引き継がせる**
- 削除・上書き・サーバ起動は、実行前に対象を提示し、終わったら停止を確認する
  （作業フォルダの `CLAUDE.md` の規則）
- 検証用の一時ファイルはスクラッチパッドへ。プロジェクトフォルダに残さない

---

## 8. 状態

第1〜第9フェーズ完了、すべて公開済み。テスト7ファイル全通過。
未コミットの変更なし。次は宿曜の4項目が本命。
